{{#if dataLoaderConsumer.useData}}
import { useData } from "{{dataLoaderConsumer.importDatafile}}";
{{/if}}

export default () => {
  {{#if dataLoaderConsumer.useData}}
  const data = useData()
  {{/if}}
  return (
    <div>
      Automatically generated Solid Page: [ {{originalPath}} ]
    </div>
  )
}

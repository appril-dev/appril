{{#if page.dataLoaderConsumer.useData}}
import { useData } from "{{page.dataLoaderConsumer.importDatafile}}";
{{/if}}

export default () => {
  {{#if page.dataLoaderConsumer.useData}}
  const data = useData()
  {{/if}}
  return (
    <div>
      Automatically generated Solid Page: [ {{page.originalPath}} ]
    </div>
  )
}

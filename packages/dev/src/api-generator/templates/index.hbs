import {
  type DefaultState,
  type DefaultContext,
  type DefinitionI,
  use,
  definitionFactory,
} from "@appril/api/router";

export type ParamsT = { {{route.params.literal}} }
export type ContextT = DefaultContext & { params: ParamsT }

export { use };

{{#each apiMethods}}
export const {{this}}: DefinitionI<
  DefaultState,
  ContextT
> = (a) => definitionFactory("{{this}}", a);

{{/each}}

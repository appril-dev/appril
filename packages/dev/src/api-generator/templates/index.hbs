import {
  type DefaultState,
  type DefaultContext,
  type DefinitionI,
  definitionFactory,
} from "@appril/api/router";

export type ParamsT = { {{route.paramsType}} }
export type ContextT = DefaultContext & { params: ParamsT }
export { use } from "@appril/api/router";

{{#each apiMethods}}export const {{this}}: DefinitionI<
  DefaultState,
  ContextT
> = (a) => definitionFactory("{{this}}", a);
{{/each}}

import routeFactory from "{{importPathmap.base}}";

export default routeFactory(({ get }) => [
  get(async (ctx) => {
    ctx.body = "Automatically generated route: [ {{route.originalPath}} ]"
  }),
])

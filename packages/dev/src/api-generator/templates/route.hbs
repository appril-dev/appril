import { get } from "{{defaults.varPrefix}}/{{defaults.apiDir}}/{{route.importPath}}";

export default [

  get(async (ctx, payload) => {
    return "Automatically generated route: [ {{route.originalPath}} ]"
  }),

]


{{#each routes}}
import {{importName}} from "{{../defaults.varPrefix}}/{{../defaults.fetchDir}}/{{../defaults.apiDir}}/{{importPath}}";
{{/each}}

export default {
  {{#each routes}}
  "{{originalPath}}": {{importName}},
  {{/each}}
}

{{BANNER}}

/** {{view.name}} view */

{{#each enums}}import type { {{declaredName}} } from "../{{../enumsDir}}/{{name}}";
{{/each}}

{{#each view.columns}}{{#if importedType}}import type { {{importedType.import}} as {{importedType.alias}} } from "{{importedType.from}}";
{{/if}}{{/each}}

export type {{view.recordName}} = {
{{#each view.columns}}

{{#each comments}}  /** {{this}} */
{{/each}}
  {{name}}: {{declaredType}};
{{/each}}
}

export type {{view.queryBuilder}} = import("@appril/dbxt").QueryBuilder<
  "{{view.moduleName}}"
>;

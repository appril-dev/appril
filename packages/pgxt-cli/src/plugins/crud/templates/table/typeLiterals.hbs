import type { Pager } from "{{importPathmap.libCrud}}";

{{#each typeLiterals}}
{{this}}
{{/each}}

export type RecordT = {
  {{#each columns}}
{{#each comments}}  /** {{this}} */
{{/each}}
  {{name}}: {{declaredType}};
  {{/each}}
}

export type InsertT = {
  {{#each columns}}
{{#each comments}}  /** {{this}} */
{{/each}}
  {{#unless isGenerated}}{{name}}{{#if isOptional}}?{{/if}}: {{declaredType}};
  {{/unless}}
  {{/each}}
}

export type UpdateT = {
  {{#each columns}}
{{#each comments}}  /** {{this}} */
{{/each}}
  {{#unless isGenerated}}{{name}}?: {{declaredType}};
  {{/unless}}
  {{/each}}
}

export type ListResponse = {
  items: Array<RecordT>;
  pager: Pager;
};

export type CreatePayload    = InsertT;
export type CreateResponse   = RecordT;
export type RetrieveResponse = RecordT;
export type UpdatePayload    = UpdateT;
export type UpdateResponse   = RecordT;
export type DeleteResponse   = RecordT;

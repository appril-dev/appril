import routeFactory, {
  type ListResponse,
  type CreateResponse,
  type InsertT,
} from "{{importPathmap.lib}}";

export default routeFactory(({ get, post }) => [
  get<never, object, ListResponse>(async (ctx) => {
    ctx.body = await ctx.crud.worker();
  }),

  post<never, { payload: InsertT }, CreateResponse>(async (ctx) => {
    ctx.body = await ctx.crud.worker(ctx.payload);
  }),
]);

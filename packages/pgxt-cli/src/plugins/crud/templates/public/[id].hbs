import {
  type RecordT,
  type UpdateT,
  get,
  patch,
  del,
} from "{{importPathmap.api}}/[id]";

export default [
  get(async (ctx, worker): Promise<RecordT> => {
    return worker();
  }),

  patch<never, { payload: UpdateT }>(async (ctx, worker): Promise<RecordT> => {
    return worker(ctx.payload);
  }),

  del(async (ctx, worker): Promise<RecordT> => {
    return worker();
  }),
];

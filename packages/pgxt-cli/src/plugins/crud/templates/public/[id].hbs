import routeFactory, {
  type RetrieveResponse,
  type UpdateResponse,
  type DeleteResponse,
  type UpdateT,
} from "{{importPathmap.lib}}/[id]";

export default routeFactory(({ get, patch, del }) => [
  get(async (ctx, worker): Promise<RetrieveResponse> => {
    return worker();
  }),

  patch<never, { payload: UpdateT }>(async (ctx, worker): Promise<UpdateResponse> => {
    return worker(ctx.payload);
  }),

  del(async (ctx, worker): Promise<DeleteResponse> => {
    return worker();
  }),
]);

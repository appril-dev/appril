import { Router } from "@solidjs/router";

import routes from "{{importPathmap.router}}/routes";
import App from "../App";
import { baseurl } from "../config";

export default () => (
  <Router root={App} base={baseurl}>
    {routes}
  </Router>
);

import { Router } from "@solidjs/router";

import routes from "{{srcFolder}}/{{defaults.varRouterDir}}/routes";
import App from "../App";
import { baseurl } from "../config";

export default () => (
  <Router root={App} base={baseurl}>
    {routes}
  </Router>
);

{
  "type": "module",
  "devPort": {{devPort}},
  "scripts": {
    "dev": "node --env-file=../.env --trace-deprecation --trace-warnings --enable-source-maps $(which pnpm) exec vite dev",
    "build": "vite build"
  }
}

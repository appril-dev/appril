import { type RecordT, type UpdateT, get, patch, del } from "{{varImportPath}}/[id]";

export default [

  get(
    (
      ctx,
      payload,
      handler,
    ): Promise<RecordT> => {
      return handler(payload)
    }
  ),

  patch(
    (
      ctx,
      payload: UpdateT,
      handler,
    ): Promise<RecordT> => {
      return handler(payload)
    }
  ),

  del(
    (
      ctx,
      payload,
      handler,
    ): Promise<RecordT> => {
      return handler(payload)
    }
  ),

]
